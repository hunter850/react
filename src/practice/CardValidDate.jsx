import { Fragment } from "react";

const CardValidDate = () => {
    const el = (
        <Fragment>
            <span>MM / YY</span>
        </Fragment>
    )

    return el;
}

export default CardValidDate