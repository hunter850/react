import { Fragment } from "react";

const CardValidDate = () => {
    const el = (
        <Fragment>
            <span style={{fontSize: "20px"}}>MM / YY</span>
        </Fragment>
    )

    return el;
}

export default CardValidDate