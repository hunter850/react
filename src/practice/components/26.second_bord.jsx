const SecondBord = ({ left, right }) => {
    const el = (
        <div>
            {left}
            {left}
            {right}
            {right}
            {right}
        </div>
    )

    return el;
}

export default SecondBord