import { Fragment } from "react";
import ThemeButton from "./38.ThemeButton";

const Toolbar = () => {
    const el = (
        <Fragment>
            <div>
                <ThemeButton />
            </div>
        </Fragment>
    )

    return el;
}

export default Toolbar